apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: mindset-app  # ✅ Changed from "study-app"
spec:
  selector:
    component: backend
  ports:
    - port: 22112
  type: LoadBalancer